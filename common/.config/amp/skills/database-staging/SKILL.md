---
name: database-staging
description: Inspect or query the CODE Hospitality staging PostgreSQL database. Use for staging data investigation, query analysis, schema inspection, and explicitly requested staging data changes.
---

# Staging Database

Use the bundled `database-staging` MCP server. Prefer schema inspection, query explanation, health analysis, and read-only SQL. Execute data or schema mutations only when the user explicitly requests them and the target is confirmed as staging.
