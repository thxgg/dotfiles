---
name: stripe-business-staging
description: Inspect or manage the CODE Hospitality business staging Stripe account. Use when the user explicitly identifies the business staging payments scope.
---

# Business Stripe — Staging

Use the bundled `stripe-business-staging` MCP server only for the business staging Stripe account. Prefer read operations. Perform Stripe writes only when the user explicitly requests the exact external action.
