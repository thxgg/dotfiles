---
name: aws
description: Inspect or operate CODE Hospitality AWS infrastructure through the code AWS profile. Use for AWS resource, regional availability, and documentation tasks.
---

# AWS

Use the bundled `aws` MCP server with the configured `code` profile. Prefer documentation and read-only AWS calls. Resource creation, modification, deletion, uploads, or scripts with side effects require an explicit user request for the exact action.
