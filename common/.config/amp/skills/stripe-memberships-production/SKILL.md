---
name: stripe-memberships-production
description: Inspect or manage the CODE Hospitality memberships production Stripe account. Load only when the user explicitly identifies the memberships production payments scope.
---

# Memberships Stripe — Production

Use the bundled `stripe-memberships-production` MCP server only for the memberships production Stripe account. Default to read-only inspection. Before any Stripe write or refund, restate the production account and exact action and obtain explicit user confirmation.
